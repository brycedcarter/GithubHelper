from setuptools import setup

setup(name='githubHelper',
      version='0.1.0',
      description='A package to assist with using the github rest api',
      author='Bryce Carter',
      author_email='bryce@tyto.com',
      packages=['githubHelper'],
      zip_safe=False)